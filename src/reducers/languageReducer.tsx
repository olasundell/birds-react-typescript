import { initialState, StoreState } from './index';
import { Action } from '../actions/actions';

export default function languageReducer(state: StoreState = initialState, action: Action): StoreState {
	return state;
}
